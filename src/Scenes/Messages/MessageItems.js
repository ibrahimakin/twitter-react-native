import React from 'react';
import { Text, View } from 'react-native';

const MessageItems = (props) => {

    return (
        <View>
            <Text>MessageItems</Text>
        </View>
    );
};

export default MessageItems;
